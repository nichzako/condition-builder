# Condition Builder — Backlog

รวม issues ที่พบและการแก้ไข ระหว่าง development แต่ละ Phase

---

## Phase 0 — Code Review Fixes (2026-05-20)

**Reviewer:** Claude Code | **Trigger:** Manual review ก่อนเริ่ม Phase 1

### [H-1] FIXED — prisma + shadcn ใน wrong dependency group

**ไฟล์:** `package.json`
**ปัญหา:** `prisma` (CLI/migration tool) และ `shadcn` (code-gen CLI) อยู่ใน `dependencies` ทำให้ถูก bundle เข้า production
**แก้:** ย้ายทั้งคู่ไป `devDependencies`
**เพิ่ม:** Prisma scripts (`prisma:generate`, `prisma:migrate`, `prisma:seed`, `db:studio`)

### [M-1] FIXED — Metadata ยังเป็น default placeholder

**ไฟล์:** `src/app/layout.tsx`
**ปัญหา:** title = "Create Next App", description = "Generated by create next app"
**แก้:** title = "Condition Builder", description = "Visual formula and condition builder"

### [M-2] FIXED — `turbopack: undefined` เป็น no-op ใน next.config.ts

**ไฟล์:** `next.config.ts`
**ปัญหา:** setting `turbopack: undefined` ไม่ทำอะไร — fix จริงคือ `--webpack` flag ใน scripts
**แก้:** ลบ property ออก อัปเดต comment ให้ชัด

### [I-1] FIXED — ขาด prisma scripts

**ไฟล์:** `package.json`
**แก้:** เพิ่ม `prisma:generate`, `prisma:migrate`, `prisma:seed`, `db:studio`

### [I-3] FIXED — .env.example ไม่ reflect format จริงของ Neon

**ไฟล์:** `.env.example`
**แก้:** อัปเดต URL format ให้ตรงกับ Neon pooler/direct pattern + comment แยก pooled vs direct ชัดเจน

### [M-3] OPEN — `@base-ui/react` vs shadcn/ui overlap

**ไฟล์:** `package.json`
**ปัญหา:** มีทั้ง `@base-ui/react` (MUI Base UI) และ `shadcn` (ใช้ Radix) — ยังไม่ชัดว่า UI layer จะใช้อะไร
**สถานะ:** เปิดไว้ตัดสินใจใน Phase 3 (Frontend Foundation) ก่อนสร้าง components จริง

---

## Open Issues (ยังไม่แก้)

| ID | Phase | Issue | Priority |
|----|-------|-------|----------|
| M-3 | 0 | `@base-ui/react` vs shadcn — ต้องตัดสินใจ UI library ใน Phase 3 | MEDIUM |

---

## หมายเหตุ

- GSD `.planning/` ยังไม่ได้ init สำหรับ project นี้ — ถ้าต้องการ structured phase tracking ให้รัน `/gsd-new-project`
- `.env.local` ถูก pull จาก Vercel แล้ว มี `DIRECT_URL` เพิ่มเข้าไปด้วยมือ (2026-05-20)
